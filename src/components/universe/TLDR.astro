---
---

<section class="tldr" data-animate-section>
  <div class="tldr-inner">
    <p class="tldr-label">TL;DR</p>
    <blockquote class="tldr-quote">
      <span data-fr>J'aime construire. Que ce soit une interface ultra design, un système automatisé complexe ou une idée un peu trop ambitieuse pour rester sur papier.</span>
      <span data-en>I love building. Whether it's a pixel-perfect interface, a complex automated system, or an idea too ambitious to stay on paper.</span>
    </blockquote>
    <p class="tldr-coda">
      <span data-fr>La ligne est mince entre un side-project, un rabbit hole gaming et une session guitare improvisée. Je ne cherche pas à la trouver.</span>
      <span data-en>The line between a side project, a gaming rabbit hole and an impromptu guitar session is thin. I stopped trying to find it.</span>
    </p>
  </div>
</section>

<style>
  .tldr {
    padding: 5rem 2rem 6rem;
    background: var(--univ-bg, #FAF7F2);
    border-top: 1px solid var(--univ-border, rgba(30,15,5,0.08));
  }

  .tldr-inner {
    max-width: 860px;
    margin: 0 auto;
    padding: 0 clamp(1rem, 5vw, 6rem);
  }

  .tldr-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--univ-accent, #E85B0A);
    margin-bottom: 1.5rem;
  }

  .tldr-quote {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3.5vw, 2.5rem);
    font-weight: 700;
    line-height: 1.25;
    color: var(--univ-text, #1A0D05);
    margin-bottom: 1.5rem;
    font-style: normal;
  }

  .tldr-coda {
    font-size: clamp(0.95rem, 1.5vw, 1.1rem);
    color: var(--univ-text-muted, rgba(30,15,5,0.45));
    line-height: 1.7;
    max-width: 560px;
  }
</style>
